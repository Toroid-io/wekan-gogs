/* This script creates an initial DB, with default configurations */

CREATE TABLE 'auth' (
	'gogs_url'		varchar,
	'gogs_username'		varchar,
	'gogs_password'		varchar,
	'gogs_token'		varchar,
	'wekan_url'		varchar,
	'wekan_username'	varchar,
	'wekan_password'	varchar,
	'w2g_url'		varchar,
	PRIMARY KEY(gogs_username)
);

CREATE TABLE 'repos' (
	'repoId'	integer,
	'username'	varchar,
	'repoName'	varchar,
	'repoFullName'	varchar,
	'boardId'	varchar,
	'backlogListId'	varchar,
	'active'	integer,
	'active_prio'	integer,
	'hookId'	integer,
	'hook_prioId'	integer,
	PRIMARY KEY(repoId)
);

CREATE TABLE 'cards' (
	'issueId'	integer,
	'repoFullName'	varchar,
	'issueIndex'	integer,
	'cardId'	varchar,
	'cardPrioId'	varchar,
	'boardId'	varchar,
	'listId'	varchar,
	'listPrioId'	varchar,
	PRIMARY KEY(issueId)
);

CREATE TABLE 'boards_prio' (
	'wekan_userid'		varchar,
	'prioBoardId'		varchar,
	'prioBacklogListId'	varchar,
	PRIMARY KEY(wekan_userid)
);

CREATE TABLE 'labels' (
	'id'		integer,
	'repoId'	integer,
	'labelName'	varchar,
	'listId'	varchar,
	PRIMARY KEY(id)
);

